# 03 Deep Dives

This directory houses detailed descriptions of project subsystems.

Currently, the primary subsystems are:
- Bluetooth check (`IOBluetoothHostController`).
- Wi-Fi resolution (`CWWiFiClient` and `getifaddrs` BSD sockets).
- Keychain test (`SecItemCopyMatching` check).
