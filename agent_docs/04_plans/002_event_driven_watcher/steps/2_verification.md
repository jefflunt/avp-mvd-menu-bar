# Step 2: Verification of Event-Driven Watcher

## Tasks
1. Compile the project:
   ```bash
   swift build
   ```
2. Build and sign the application package:
   ```bash
   ./package.sh
   ```
3. Run the packaged `.app` bundle and interact with system controls (Bluetooth, Wi-Fi, Screen Lock) to confirm real-time updates.
