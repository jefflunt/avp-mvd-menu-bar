# Step 1: Project Setup

Establish the project structure using Swift Package Manager to easily compile our macOS utility.

## Tasks
1. Create `Package.swift` file for a command line / macOS executable target.
2. Structure the directory for source files: `Sources/`.
3. Verify compilation of a basic "Hello World" executable.

## Files
- `Package.swift` [NEW]
