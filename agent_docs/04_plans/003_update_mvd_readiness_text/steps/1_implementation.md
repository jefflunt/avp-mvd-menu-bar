# Step 1: Update MVD Readiness Text & Screenshot

## Tasks
1. Update `Sources/AVPMVDMenuBarApp.swift`:
   - Replace `Text("Mac Virtual Display Status")` with `Text("Mac Virtual Display Readiness")`.
2. Compile and package the application:
   - Run `scripts/package.sh` to produce `AVPMVDMenuBar.app`.
3. Launch the updated application:
   - Open `AVPMVDMenuBar.app`.
4. Provide a reminder in the final walkthrough for the user to capture the new screenshot and overwrite `images/avp-mvd-screenshot.png`.
