# Step 2: Verification of Version Number Display

## Tasks
1. Run the packaged `.app` bundle:
   - `open .build/release/AVPMVDMenuBar.app`
2. Open the menu bar dropdown and verify that the version string is displayed as expected (e.g., "Version: v1" or "v1").
3. (Optional) Run the raw binary directly and verify the same version displays properly.
